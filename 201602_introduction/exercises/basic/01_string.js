// Create a function that returns the initials of a person
// HINT: .charAt(index) lets you treat a string like an array of characters
Initials(firstName,lastName){
  console.log(firstName.charAt(1)+lastName.charAt(1))
}
